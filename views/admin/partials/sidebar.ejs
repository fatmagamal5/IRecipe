<!-- Sidebar -->
<div class="col-auto col-md-3 col-xl-2 px-sm-2 px-0" style="background: linear-gradient(to bottom, var(--heading-color), #1a1a1f);">
  <div class="d-flex flex-column align-items-center align-items-sm-start px-3 pt-2 text-white min-vh-100">
    <a href="/admin/dashboard" class="d-flex align-items-center pb-3 mb-md-0 me-md-auto text-white text-decoration-none">
      <span class="fs-5 d-none d-sm-inline" style="font-family: var(--heading-font);">Admin Panel</span>
    </a>
    <ul class="nav nav-pills flex-column mb-sm-auto mb-0 align-items-center align-items-sm-start w-100" id="menu">
      <li class="nav-item w-100">
        <a href="/admin/dashboard" class="nav-link align-middle px-3 py-3 text-white d-flex align-items-center" style="transition: all 0.3s ease;">
          <i class="fs-4 bi-house me-2"></i>
          <span class="ms-1 d-none d-sm-inline" style="font-family: var(--nav-font);">Dashboard</span>
        </a>
      </li>
      <li class="nav-item w-100">
        <a href="/admin/recipes" class="nav-link px-3 py-3 align-middle text-white d-flex align-items-center" style="transition: all 0.3s ease;">
          <i class="fs-4 bi-book me-2"></i>
          <span class="ms-1 d-none d-sm-inline" style="font-family: var(--nav-font);">Recipes</span>
        </a>
      </li>
      <li class="nav-item w-100">
        <a href="/admin/users" class="nav-link px-3 py-3 align-middle text-white d-flex align-items-center" style="transition: all 0.3s ease;">
          <i class="fs-4 bi-people me-2"></i>
          <span class="ms-1 d-none d-sm-inline" style="font-family: var(--nav-font);">Users</span>
        </a>
      </li>
      <li class="nav-item w-100">
        <a href="/api/user/logout" class="nav-link px-3 py-3 align-middle text-white d-flex align-items-center" style="transition: all 0.3s ease;">
          <i class="fs-4 bi-box-arrow-right me-2"></i>
          <span class="ms-1 d-none d-sm-inline" style="font-family: var(--nav-font);">Logout</span>
        </a>
      </li>
    </ul>
  </div>
</div>

<style>
  .nav-link {
    border-radius: 8px;
    margin: 4px 0;
    position: relative;
    overflow: hidden;
  }

  .nav-link::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, var(--accent-color), var(--nav-hover-color));
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 0;
  }

  .nav-link:hover::before {
    opacity: 1;
  }

  .nav-link i, .nav-link span {
    position: relative;
    z-index: 1;
    transition: all 0.3s ease;
  }

  .nav-link:hover i {
    transform: scale(1.1);
  }

  .nav-link:hover span {
    transform: translateX(5px);
  }

  .nav-link.active {
    background: linear-gradient(45deg, var(--accent-color), var(--nav-hover-color));
  }

  .nav-link.active::before {
    opacity: 1;
  }
</style>

<script>
  // Add active class to current page link
  document.addEventListener('DOMContentLoaded', function() {
    const currentPath = window.location.pathname;
    const navLinks = document.querySelectorAll('.nav-link');
    
    navLinks.forEach(link => {
      if (link.getAttribute('href') === currentPath) {
        link.classList.add('active');
      }
    });
  });
</script>
